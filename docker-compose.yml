version: '3.1'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: passwd
      POSTGRES_USER: user
      POSTGRES_DB: wows-game-notifier
    ports: 
      - 5432:5432
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080